name: potato_market
description: A new Flutter project.


version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider:

  firebase_core:
  firebase_auth:
  firebase_storage:
  cloud_firestore:
  
  multi_image_picker:
  image_picker:
  modal_progress_hud:
  splashscreen:
  hive:
  hive_flutter:
  geolocator:
  google_maps_flutter:
  geoflutterfire:
  http:
  intl_phone_number_input:

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator:
  build_runner:

flutter:
  assets:
    - assets/splash_image.jpg
    - assets/start_image.jpg
    - assets/default_user.jpg
  uses-material-design: true